= render :partial => "/shared/errors", :object => @schooling.errors
- form_for(@schooling, :builder => MyFormBuilder, :html => { :multipart => true }) do |f|
  = render :partial => 'form', :locals => { :f => f}
