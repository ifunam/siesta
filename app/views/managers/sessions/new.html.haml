%h2 Sign in
= form_for(resource, :as => resource_name, :url => manager_session_path(resource_name)) do |f|
  %p
    = f.label :login
    %br/
    = f.text_field :login
  %p
    = f.label :password
    %br/
    = f.password_field :password
  %p= f.submit "Sign in"
