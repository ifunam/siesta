<%= error_messages_for :user %>
<div class="title"> Inicio de sesión </div>
<% form_for :user, :url => { :action => "login" } do |f| %>
  <p>
    <b>Usuario:</b> <%= f.text_field :login, :size => 12 %>
  </p>

  <p>
    <b>Contraseña:</b> <%= f.password_field :passwd, :size => 12%>
  </p>

  <p>
    <%= f.submit "Entrar" %>
  </p>
<% end %>
<hr />

<p>¿No tiene una cuenta en el sistema?</p>
<%= link_to_remote "Obtenga una ahora", :update => 'form', :url => { :action => "update_partial", :partial => 'new' } %>

<hr />
<p>¿Ha olvidado su contraseña?</p>
<%= link_to_remote "Cambie su contraseña", :update => 'form', :url => { :action => "update_partial", :partial => 'recovery_passwd_request' } %>
