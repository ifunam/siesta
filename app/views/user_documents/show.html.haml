%h3= t '.title'
= render :partial => 'user_documents/user_document_show', :object => @user_document

.span12
  = link_to t(:edit), edit_user_document_path, :class => 'btn btn-primary'
  = link_to t(:back), user_documents_path, :class => 'btn'
